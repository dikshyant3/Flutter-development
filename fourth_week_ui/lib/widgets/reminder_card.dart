import 'package:flutter/material.dart';

class ReminderMessage extends StatelessWidget {
  const ReminderMessage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}