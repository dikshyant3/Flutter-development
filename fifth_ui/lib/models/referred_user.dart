class ReferredUser {
  final String name;
  final String avatarUrl;
  final bool isVerified;

  ReferredUser({
    required this.name,
    required this.avatarUrl,
    required this.isVerified,
  });
}